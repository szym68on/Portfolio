function Main({ children }) {
  return (
    <main className="md:w-[60%] mx-auto md:mt-12 mt-[20px] w-[90%] flex flec-col md:flex-row justify-center md:justify-normal">
      {children}
    </main>
  );
}

export default Main;
